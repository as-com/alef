enum StyleType {
	RULE,
	KEYFRAME,
	FONT,
	STATIC,
	CLEAR
}

export default StyleType;

export const RULE_TYPE = StyleType.RULE;
export const KEYFRAME_TYPE = StyleType.KEYFRAME;
export const FONT_TYPE = StyleType.FONT;
export const STATIC_TYPE = StyleType.STATIC;
export const CLEAR_TYPE = StyleType.CLEAR;

// TODO: Refactor into enum
